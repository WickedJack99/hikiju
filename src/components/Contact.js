import Layout from "./Layout";
import * as React from 'react';

function Contact() {
    const content = (
        <>
            
        </>   
    );

    return (
        <Layout content={content}/>
    );
}

export default Contact;